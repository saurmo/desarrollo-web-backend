info:
  name: get-all users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/users?page=2&limit=30
  params:
    - name: page
      value: "2"
      type: query
    - name: limit
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
