
const PGUSER='postgres' 
const PGHOST='localhost'
const PGPASSWORD='admin123' 
const PGDATABASE='reservas' 
const PGPORT='5433' 

module.exports = {
    PGUSER,
    PGHOST,
    PGPASSWORD,
    PGDATABASE,
    PGPORT
}

